@if(!string.IsNullOrEmpty(ViewData["UserLog"].ToString()))
{
<div class="right" style="position: absolute; right: 36px; top: 36px;">
    <div class="dropdown">
        <span class="" id="dropdownMenuButton" data-toggle="dropdown" style="cursor: pointer;">
            <i class="fa fa-user"></i> @ViewData["UserLog"]
        </span>
        <div class="dropdown-menu">
            <a class="dropdown-item" asp-controller="Login" asp-action="Logout">Sair</a>
            @*<a class="dropdown-item" href="#">Another action</a>
            <a class="dropdown-item" href="#">Something else here</a>*@
        </div>
    </div>
</div>
}